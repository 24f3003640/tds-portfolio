name: TDS GA1 Validation

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: '24f3003640@ds.study.iitm.ac.in'
        run: echo "Hello, world!"
